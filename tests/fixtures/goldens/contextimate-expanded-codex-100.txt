
[Context Estimator] summary → compact → expanded
  <expand-key>: cycle view · model openai-codex/gpt-5.5 · OpenAI-Codex heuristic
  Expanded view: per-section subtotals, sources, and a readable schema breakdown of each active tool.
  Total harness                              ~0.4k tokens (2.3k chars)

  Tool outputs                              ~13k tokens (52.3k chars/4)
  Messages                                   ~2k tokens (8.1k chars/4)
  Other / reasoning                         ~49k tokens (residual)
  Total session                             ~64k tokens (Pi current - harness)
  Total request                              64k tokens (32.2% / 200k ctx)

  Runtime system prompt  ~0.1k tokens                                             201 chars · text/4
    assembled at runtime: pi base prompt + tool/extension instructions · preview only
    of which tool/extension instructions: ~0.0k tokens (2 tool snippets, 1 guideline) · already counted in this row

    You are a fixture harness for pine-of-glass tests.
    Available tools:
    - read: Read file contents
    - bash: Execute bash commands
    Guidelines:
    - Prefer rg over grep for searching.

  Global AGENTS.md  ~0.0k tokens                                                   64 chars · text/4
    ~/.pi/agent/AGENTS.md · preview only

    # Global guidance
    Prefer clarity &amp; precision in every reply.

  ~/projects/demo/AGENTS.md  ~0.0k tokens                                          52 chars · text/4
    ~/projects/demo/AGENTS.md · preview only

    # Demo project
    Run the demo suite before committing.

  Skill frontmatter (3)  ~0.2k tokens                                             743 chars · text/4
    list wrapper/markup  ~0.0k tokens (195 chars/4)

    alpha-skill  ~0.1k  Handles A & B cases with a long description so it sorts first in token orde…
    beta-skill   ~0.0k  It's the medium one.
    gamma        ~0.0k  Tiny.

  Tools (3/4 active)  ~0.2k tokens                             1.3k chars · payload · OpenAI formula
    formula +7/fn +3/prop-section +3/prop -3/enum +3/enum-item +12 once · nested counted recursively
    counted on the minified provider payload (1.3k chars); tree below is the readable view of it

    search · user · npm:fixture-pack · package · ~/.pi/agent/npm/fixture/search.ts             ~0.1k
      Search the web with one or more queries.
      queries       array   required  Queries searched in sequence
        text        string  required  Query text
        recency     string            Recency filter
      options       object            Search options
        numResults  number            Results per query

    read · temporary · builtin · top-level · <builtin:read>                                    ~0.0k
      Read the contents of a file.
      path    string  required  Path to the file to read
      offset  number            Line number to start reading from
      limit   number            Maximum number of lines to read

    bash · temporary · builtin · top-level · <builtin:bash>                                    ~0.0k
      Execute a bash command in the current working directory.
      command  string  required  Bash command to execute
      timeout  number            Timeout in seconds
