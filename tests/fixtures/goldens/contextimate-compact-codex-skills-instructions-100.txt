
[Contextimate] summary → compact → expanded
  <expand-key>: cycle view · model openai-codex/gpt-5.5 · counts ch ÷ 4 · tools: OpenAI formula (OpenAI-Codex heuristic)

  ▸ Runtime system prompt  ~0.0k tokens (0.2k ch)

  ▸ Global AGENTS.md       ~0.0k tokens (0.1k ch)

  ▸ Skill frontmatter (3)  ~0.1k tokens (0.5k ch)
    alpha-skill            ~0.0k  Handles A & B cases with a long description so it sorts first in…
    beta-skill             ~0.0k  It's the medium one.
    gamma                  ~0.0k

  ▸ Tools (1/4 active)     ~0.1k tokens (0.6k ch)
    search                 ~0.1k  Search the web with one or more queries.
    bash                    -     (inactive) Execute a bash command in the current working director…
    read                    -     (inactive) Read the contents of a file.
    write                   -     (inactive) Write content to a file.

  Total harness            ~0.3k tokens (1.3k ch · <1% ctx)

  Tool outputs                              ~13.1k tokens (52.3k ch)
  Messages                                   ~2.0k tokens (8.1k ch)
  Thinking summaries                         ~0.3k tokens (1.2k ch)
  Reasoning context                          12.0k tokens (provider)
  Unattributed                              ~36.7k tokens (accounting gap)
  Total session                             ~64.1k tokens (~32% ctx · Pi-based)
  Total request                              64.3k tokens (32.2% / 200k ctx)
  █████████▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒  harness ~0.3k · session ~64.1k · free 135.7k
