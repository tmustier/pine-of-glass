
[Context Estimator] summary → compact → expanded
  <expand-key>: cycle view · model openai-codex/gpt-5.5 · OpenAI-Codex heuristic
  Scan view: one line per skill/tool, sorted by estimated tokens · Ctrl+O for full detail

  ▸ System prompt              ~0.0k tokens (137 chars/4)

  ▸ Global AGENTS.md           ~0.0k tokens (64 chars/4)

  ▸ ~/projects/demo/AGENTS.md  ~0.0k tokens (52 chars/4)

  ▸ Skill frontmatter (3)      ~0.2k tokens (743 chars/4)
    alpha-skill                ~0.1k  Handles A & B cases with a long description so it sorts first…
    beta-skill                 ~0.0k  It's the medium one.
    gamma                      ~0.0k  Tiny.

  ▸ Tools (3/4 active)         ~0.2k tokens (1.3k chars · payload size; OpenAI-style local formula; schema text/6.6)
    search                     ~0.1k  Search the web with one or more queries.
    read                       ~0.0k  Read the contents of a file.
    bash                       ~0.0k  Execute a bash command in the current working directory.
    write                       -     (inactive) Write content to a file.

  Total harness                ~0.4k tokens (2.3k chars)

  Tool outputs                              ~13k tokens (52.3k chars/4)
  Messages                                   ~2k tokens (8.1k chars/4)
  Other / reasoning                         ~49k tokens (residual)
  Total session                             ~64k tokens (Pi current - harness)
  Total request                              64k tokens (32.2% / 200k ctx)
